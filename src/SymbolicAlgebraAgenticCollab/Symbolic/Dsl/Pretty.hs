module SymbolicAlgebraAgenticCollab.Symbolic.Dsl.Pretty (
    PrettyContract (..),
    PrettyTraceContract (..),
) where

data PrettyContract = PrettyContract
    deriving (Eq, Show)

data PrettyTraceContract = PrettyTraceContract
    deriving (Eq, Show)
